# Command palette layer: one row, no config. The host half is an empty
# plugin (presence-only); the browser half ships the palette UI through the
# package's dsh.client declaration.
- insert:
    - id: ui-command-palette
      name: dsh-command-palette
